import React from 'react'

// Can make this into a modal later

const HelpModal = () => {
  return (
    <div>
      <button className='bg-blue-100 rounded-full font-semibold flex items-center justify-center w-6 h-6 text-blue-500'>?</button>
    </div>
  )
}

export default HelpModal
